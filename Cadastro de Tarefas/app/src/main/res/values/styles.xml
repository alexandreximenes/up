<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Base.Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="estiloBtnEditar" parent="@android:style/Widget.Button">
        <item name="android:background">@drawable/btn_editar</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">#616161</item>
    </style>

    <style name="estiloBtnConcluir" parent="@android:style/Widget.Button">
        <item name="android:background">@drawable/btn_concluir</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">#ccc7c7</item>
    </style>
</resources>
